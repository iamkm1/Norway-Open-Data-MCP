allowBuilds:
  # esbuild is the compiler behind tsup and vitest. It is a build-time only
  # dependency and never ships in the published package.
  esbuild: true

minimumReleaseAgeExclude:
  - norway-open-data-sdk@0.5.2 || 0.5.3 || 0.6.0 || 0.7.0 || 0.8.0
